package pl.agh.stockexchange.exchange;

import java.util.concurrent.TimeUnit;

public interface Constants {
    long TICKING_INTERVAL = 1000;
    TimeUnit DEFAULT_TIME_UNIT = TimeUnit.MILLISECONDS;
}
